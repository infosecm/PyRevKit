# Requirements for WebSocket Relay Server with Authentication
# Python 3.7+

# Main dependency - WebSocket library
websockets>=12.0

# All other modules are part of Python standard library:
# - asyncio (async/await support)
# - ssl (TLS/SSL encryption)
# - hashlib (PBKDF2 password hashing)
# - secrets (cryptographically secure random numbers)
# - json (credential file storage)
# - argparse (command-line argument parsing)
# - getpass (secure password input)
# - pathlib (file path handling)
# - sys (system operations)
